server:
    port: 8092

spring:
    application:
        name: "loans"
    profiles:
      active: "prod"
      
#uncomment for mysql 
#    datasource:
#        url: jdbc:mysql://localhost:3307/loansdb
#        username: root
#        password: root
#    jpa: 
#        show-sql: true
#    sql:
#      init:
#        mode: always
#
#end of mysql

#for H2
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password: ''
    h2:
        console:
            enabled: true
    jpa: 
        database-platform: org.hibernate.dialect.H2Dialect
        hibernate:
            ddl-auto: update
        show-sql: true
#
## end of H2

    config:
      import: "optional:configserver:http://localhost:8072/"
#    rabbitmq:
#      host: "localhost"
#      port: 5672
#      username: "guest"
#      password: "guest"
       
management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readiness-state:
      enabled: true
    liveness-state:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

endpoints:
  shutdown:
    enabled: true

eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
      
info:
  app:
    name: "loans"
    description: "jayslabs loans microservice"
    version: "1.0.1"
    
logging:
 level:
   jayslabs:
     microservicedemo:
       loans: DEBUG